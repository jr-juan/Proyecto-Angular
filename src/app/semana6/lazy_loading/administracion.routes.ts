import { Routes } from '@angular/router';
import { PanelAdminComponent } from './panel_admin.component';


export const ADMIN_ROUTES: Routes = [
  { path: '', component: PanelAdminComponent }, // va hacia la vista del panel de administración
];
